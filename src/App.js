const { Console } = require("@woowacourse/mission-utils");

class App {
    // 입력받아 결과 출력
    // Customer : getPurchaseAmount(money) => 로또 수량 amount 반환
    // Generator : getLottos(amount) => 로또 배열 lottos 반환
    // Lotts : getNumbers => 로또 배열 numbers 반환

    play() {
        Console.readLine();
    }
}

module.exports = App;
